# Local dev PostgreSQL credentials (consumed by compose.yaml and up.ps1).
# These are intentionally weak — this stack is for local development only.
POSTGRES_USER=binstash
POSTGRES_PASSWORD=binstash_dev_pw
POSTGRES_DB=binstash
# Host port; 5433 avoids clashing with a Postgres already on 5432.
POSTGRES_PORT=5433
