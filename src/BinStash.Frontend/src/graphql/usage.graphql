query TenantUsage {
  tenantUsage {
    tenantId
    logicalBytes
    repositoryCount
    releaseCount
    maxStorageBytes
    isLimited
    storageUsedFraction
    isStorageAllowed
    isIngestAllowed
    isEgressAllowed
  }
}
