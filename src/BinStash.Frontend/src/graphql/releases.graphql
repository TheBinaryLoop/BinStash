query Release($id: UUID!) {
  release(id: $id) {
    id
    version
    createdAt
    notes
    repoId
    customProperties
    repository { id name storageClass }
    metrics {
      chunksInRelease
      totalLogicalBytes
      metaBytesFull
      componentsInRelease
      filesInRelease
    }
  }
}
